# Campus Marketplace

This project is a complete MERN stack Campus Marketplace application with a retro UI aesthetic. It allows users to buy, sell, and communicate regarding items on campus.

## Features
- User Authentication
- CRUD Operations for Items
- Messaging between Users
- Reviews for Items

## Getting Started

### Prerequisites
- Node.js
- MongoDB

### Installation
1. Clone the repository.
2. Navigate to the backend directory:
   ```bash
   cd backend
   ```
3. Install the backend dependencies:
   ```bash
   npm install
   ```
4. Create a .env file in the backend directory and add your MongoDB URI.
5. Start the backend server:
   ```bash
   npm start
   ```

6. Navigate to the frontend directory:
   ```bash
   cd frontend
   ```
7. Install the frontend dependencies:
   ```bash
   npm install
   ```
8. Start the frontend application:
   ```bash
   npm start
   ```

## Folder Structure
- `backend/`
  - `models/`
  - `routes/`
  - `controllers/`
  - `middleware/`
  - `.env`
  - `server.js`
  - `package.json`

- `frontend/`
  - `src/components/`
  - `src/pages/`
  - `src/styles/`
  - `App.jsx`
  - `package.json`

## Author
- divyakothari4